# ============================================================================
# 运行时与构建产物不进版本库 —— 它们是"绿色版压缩包"的一部分，放 Releases 里
# ============================================================================
运行环境/
# 构建脚本本身进版本库（方便别人自己重打包），但构建产物不进
构建/windows/
构建/发布/
构建/启动器/*.o

# 例外：Windows 启动器要跟着仓库走
#   · 它得用 mingw 交叉编译，CI 上现装工具链太慢；
#   · 175 KB，已在 wine 下实测能拉起自带的 pythonw.exe（不弹控制台）。
!构建/启动器/启动.exe
更新/
发布/

# 用户自己的东西
配置.json
数据/
适配器/*/数据/

# Python 缓存
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# 编辑器
.idea/
.vscode/
*.swp

# CI 工作流暂时不入库：推 workflow 文件需要令牌带 workflow 作用域，
# 当前令牌只有 repo，推了会让整条推送被拒。等加上作用域后删掉这一行即可。
.github/workflows/release.yml

# 测试时留下的进程记录（自检脚本产物，别入库）
$记录

# 一键启动快捷方式：工作区本地产物（里面是本机绝对路径与用户名，不进仓库）
一键启动_网盘管理.sh
一键启动_网盘管理.desktop
一键启动_网盘管理.bat
网盘管理_图标.png
