# v8_3/播放/__init__.py
"""视频播放子系统（V8_3 新增）。

* :mod:`vlc绑定`  —— 用 ctypes 直接驱动系统 libvlc（不依赖 python-vlc）
* :mod:`媒体信息`  —— ffprobe 探测分辨率/码率/音字幕轨
* :mod:`直链探测`  —— 实测 TTFB / 带宽 / Range 支持，供 AI 选起播参数
* :mod:`参数决策`  —— 规则兜底 + 调用 AI 播放顾问
* :mod:`播放核心`  —— 播放会话：解析直链 → 起播 → 控制 → 统计
* :mod:`语音识别`  —— 本地 Whisper（由独立支线提供，可能尚未就绪）
"""
